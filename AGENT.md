# Agent Guide

- `src/YYYYMMDD/`를 원본으로, `docs/YYYYMMDD/`와 `docs/index.html`을 배포본으로 유지한다.
- 원문 전체 복제 대신 요약·분석 중심으로 작성한다.
- 이미지 경로는 날짜별 `./images/...` 기준으로 검증한다.
- 모바일 가독성: viewport, media query, keep-all, overflow-wrap, responsive image CSS를 유지한다.
