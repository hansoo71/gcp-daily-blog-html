# Agent Notes

- 화면 표시 텍스트와 접근성 라벨은 한국어 중심으로 유지합니다.
- 원문 URL, 파일 경로, 공식 기술명은 출처 보존과 검색성을 위해 원문 표기를 허용합니다.
- `src/YYYYMMDD`, `docs/YYYYMMDD`, `docs/index.html`, root fallback을 함께 동기화합니다.
- 이미지 자산은 날짜별 `images/` 폴더에 저장하고, 최신 `docs/index.html`에서는 날짜별 상대경로를 사용합니다.
